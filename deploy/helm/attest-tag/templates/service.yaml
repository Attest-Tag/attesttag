apiVersion: v1
kind: Service
metadata:
  name: {{ include "attest-tag.fullname" . }}
  labels: {{- include "attest-tag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "attest-tag.selectorLabels" . | nindent 4 }}
