import type { RecutLongForm } from "../recut";
import { DEEP_DIVE_RECUT } from "./deep-dive";

/** Every walkthrough that has a re-cut of a recorded take, by id. */
export const RECUTS: RecutLongForm[] = [DEEP_DIVE_RECUT];
